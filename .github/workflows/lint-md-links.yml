---
name: Lint MD and Links

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  issues: write  # for the reusable workflow's optional notify job

jobs:
  lint:
    uses: qte77/.github/.github/workflows/lint-md-links.yml@4801217a20922cd54187fc7e4c98c7ea78c0ec9a  # 2026-09-19 (bumped from 55ea1a99 to fix startup_failure — matches the pin already used by bump-version.yml/tag-release.yml/publish-release.yml)
...
